@model DBContext
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Fatto</h1>
    <h2>La tua prenotazione è avvenuta con successo</h2>
    <h3>Ecco la tua prenotazione</h3>
    <h4> @Html.ActionLink("Per cancellare tutti i dati clicca qui","CancellaTutto","Home",null,null)</h4>

    <table class="table table-bordered table-striped table-sm">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Piani</th>
            </tr>
        </thead>
            
        <tbody>
            @foreach (Condominio a  in Model.Condomini)
            {
                <tr>
                    <td>@a.Nome</td> 
                    <td>@a.Piani</td> 
                    <td>@Html.ActionLink("cancella","Cancella","Home",new {id=a.CondominioId},null)</td>
                </tr>
            }
        </tbody>
    </table>
</div>